pipeline {
    agent any
    stages {
        stage('Get_Playbook') {
            steps {
                git 'https://gitlab.com/jaya-narayana/devnetopscicdpipeline.git'
            }
        }
        stage('Run_Playbook') {
            steps {
                sh 'ansible-playbook playbook.yml'
            }
        }
    }
}
